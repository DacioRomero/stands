import React from 'react';
import { Hero, HeroBody, Container } from 'bloomer';

export default () => (
  <Hero>
    <HeroBody>
      <Container>
        Hello World!
      </Container>
    </HeroBody>
  </Hero>
);
